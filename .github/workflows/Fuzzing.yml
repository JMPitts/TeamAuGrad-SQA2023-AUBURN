name: Fuzzing Build

on:
  push:
    branches: [ slikube-enhancement, master, Fuzzing ]
  pull_request:
     branches: [ master ]

jobs:
  Fuzzing-Methods:
    runs-on: ubuntu-latest
    Steps:
    - name: Fuzzing methods to find errors.
      run: |
        python fuzz.py
